import React from 'react';
import ReactDOM from 'react-dom';
import Register from '../views/sign/register';
import '../css/main.scss';

const rootEl = document.getElementById('root');
ReactDOM.render(<Register />, rootEl);

